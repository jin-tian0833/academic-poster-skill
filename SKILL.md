---
name: academic-poster
description: 阅读论文 PDF 并生成单文件 HTML 学术海报（微信公众号科研推文/学术长图风格）。当用户上传论文 PDF 并要求"生成学术海报"、"论文海报"、"论文解读推文"或"paper poster"时使用此 Skill。自动阅读论文、提炼内容要素、解析关键图表，最终输出一个可在浏览器离线打开的单文件 .html，并支持迭代修改。无额外依赖。
---

# 学术海报

将一篇论文 PDF 转化为一份纵向长页面、手机优先的单文件 HTML 学术海报。核心价值：不机械翻译论文结构，而是重组为"为什么重要 → 作者做了什么 → 得到什么结果 → 图表提供了什么证据 → 意味着什么"的科研叙事，并配上论文关键 Figure/Table 的图文解析。首版渲染视为草稿，迭代修改是核心体验。

## 工作流程

1. **阅读原文。** 提供了 PDF 就直接用 Read 工具读取（多模态，逐页）；只粘贴了摘要或草稿就基于现有内容做。绝不编造原文没有的结果或数字——错误陈述结论的海报不如朴素的海报。阅读时提炼以下要素：标题/作者/期刊/年份/DOI、研究背景、核心问题、3–5 个贡献、方法框架、关键结果及对应 Figure/Table 编号与图中数据、机制分析（如有）、结论与意义。
2. **提炼海报要素。** 按下文"内容组织"的叙事模块和图表五要素规则组织内容，不按 Introduction/Methods/Results/Discussion 机械翻译。
3. **选择图表。** 优先使用论文原始 Figure，直接从 Read 读到的页面中看图并判断。默认以文字方式做图表解析并标明"Figure N"原文编号；若用户另行提供了图片文件或明确要求嵌入原图位图，再将其转为 base64 data URI 嵌入。禁止重绘、篡改数据图形，不得改变数据含义。
4. **渲染单文件 HTML。** 以 `assets/poster-template.html` 为起点（全部 CSS 已内联：纵向长页面、手机优先、图文交替、单一强调色、浅色背景深色正文；不引入外部框架/CDN/字体）。图片以 base64 data URI 嵌入，支持离线打开。文件名用短格式：`<第一通讯作者姓>_<期刊缩写>_<年份>.html`，如 `Wang_JACS_2025.html`（通讯作者未标注时取第一作者；期刊缩写不确定时用原刊名截短，不猜测；文件名不含空格和特殊字符）。输出真正的 .html 文件，不要只展示代码。
5. **提供交互式修改。** 用 present_files 交付后，主动邀请用户修改任意模块（如"方法部分精简一点"、"换冷色调强调色"、"这组图换个角度解析"），按需求局部调整后重新生成。把第一版当作草稿而非成品。

## 内容组织

按以下顺序组织叙事模块（"机制/深入分析"仅在论文确实存在时保留；论文没有的内容不补造）：

1. **标题区**：论文标题、作者、期刊/会议、年份。
2. **研究背景**：为什么研究这个问题，2–4 句。
3. **核心问题**：论文具体解决什么问题，1–3 句，引用块突出。
4. **工作亮点**：3–5 条最重要贡献，每条一句话。
5. **方法/研究框架**：实验流程、模型结构、材料设计等，复杂流程分步讲解。
6. **核心结果图文解析**：选 3–6 组最重要的 Figure/Table，图文交替排列。
7. **机制/深入分析**（可选）。
8. **总结与科研意义**：2–4 句。
9. **Take-home Message**：1–2 句话，引用块突出。
10. **文献信息**：完整引用，只使用 PDF 中实际存在的信息。

**每组核心图的解析必须包含五要素**（与是否嵌入原图无关）：

1. 这张图研究什么
2. 应该重点看哪里（文字指引导读）
3. 主要结果是什么
4. 结果意味着什么
5. 一句话 Takeaway

## 学术红线（违反任何一条必须返工）

- 不编造实验数据、统计结果、实验条件、样本量、结论、作者观点、DOI 或引用
- "显著提高"、"首次"、"SOTA"、"提升 XX%"等表达必须有论文原文明确支持，否则用中性表述
- 嵌入的图表来自论文原始 PDF 且未篡改数据含义；未嵌入的图只做文字解析并标明原文编号，不重绘
- 引用信息与 PDF 一致，缺失字段标"未标注"而非猜测

## 交付前自检

- 30 秒能知道论文研究什么；几分钟能理解主要贡献
- 每张关键图都讲清"说明了什么"（五要素齐全）
- 不读原文也能理解核心科研故事
- HTML 单文件离线打开正常，手机/桌面显示正常

## 资源说明

- `assets/poster-template.html`：海报 HTML/CSS 骨架模板，含占位区块与全部内联 CSS，直接复制修改。
